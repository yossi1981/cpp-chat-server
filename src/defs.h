#ifndef CHAT_SERVER_DEFS
#define CHAT_SERVER_DEFS
#include <string>
#include <iostream>

using namespace std;
using Port = unsigned short;
using IpAddress = string;
using SessionToken = string;
#define E(msg) cout<<msg<<endl;

#endif